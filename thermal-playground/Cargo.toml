[package]
name = "thermal-playground"
version = "0.1.0"
edition = "2024"

[build-dependencies]
quote = "1.0.45"

[dependencies]
png = "0.18.1"
thermal = { path = "../thermal", features = ["rwh"] }
winit = "0.30.13"

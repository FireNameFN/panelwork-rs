[package]
name = "slang-builder"
version = "0.1.0"
edition = "2024"

[dependencies]
shader-slang = { git = "https://github.com/FloatyMonkey/slang-rs" }
#shader-slang = { git = "https://github.com/ThierryBerger/slang-rs", branch = "static-build", default-features = false, features = ["static"] }
